package org.example.intelligence;

public class ModalAI {
    public void executarTarefa(String tarefa) {
        // Lógica para executar uma tarefa de inteligência artificial com base na entrada fornecida
        if (tarefa != null && !tarefa.isEmpty()) {
            // Implemente a lógica da tarefa de inteligência artificial aqui
            System.out.println("Tarefa de inteligência artificial executada com sucesso.");
        } else {
            System.out.println("A entrada fornecida está vazia. Nenhuma tarefa executada.");
        }
    }
}
